# ─── Core Training Stack ──────────────────────────────────────
# GPU (Linux): pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPU / Windows: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.3.0
transformers>=4.44.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.10.0
accelerate>=0.33.0

# bitsandbytes: required for 4-bit quantization (QLoRA)
#   Linux/WSL: pip install bitsandbytes>=0.43.0
#   Windows native: pip install bitsandbytes  (official Windows support since 0.43)
#   Fallback: training runs in fp32 if not installed (very slow)
bitsandbytes>=0.43.0

# Unsloth: 2-5x faster training (Linux + CUDA only — skip on Windows)
# pip install unsloth
# unsloth

# ─── Utilities ───────────────────────────────────────────────
pyyaml>=6.0
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.5.0
tqdm>=4.66.0

# ─── Logging & Monitoring ────────────────────────────────────
tensorboard>=2.17.0
wandb>=0.17.0          # Optional: experiment tracking

# ─── Evaluation & API ────────────────────────────────────────
requests>=2.32.0
evaluate>=0.4.0        # HuggingFace evaluate metrics
rouge_score>=0.1.2

# ─── Notebook Support ────────────────────────────────────────
jupyter>=1.0.0
ipywidgets>=8.1.0
matplotlib>=3.9.0
seaborn>=0.13.0
